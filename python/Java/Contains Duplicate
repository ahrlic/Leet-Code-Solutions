#217

class Solution {
    public boolean containsDuplicate(int[] nums) {
    var duplicates = new HashSet<>();

    for (int num: nums){
        if (duplicates.contains(num)){
            return true;
        }
        duplicates.add(num);
    }
     return false;
}
}
